/**
 * TermiPet - Main Entry Point
  * An educational, themeable terminal companion framework
   */

   const Hatchery = require('./core/hatchery');
   const { renderPet } = require('./renderer/theme');

   module.exports = {
     Hatchery,
       renderPet
       };
